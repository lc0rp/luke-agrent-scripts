interface:
  display_name: "Direct Interactions"
  short_description: "Triage high-signal interactions meant for Luke"
  default_prompt: "Use $direct-interactions to fetch actionable mail, Slack, and Granola interactions meant for me."

policy:
  allow_implicit_invocation: true
