{
  "id": "run-command",
  "name": "Run Command",
  "description": "Adds /run command for shell execution plus quick-command shortcuts (/run help, +q, -q, q)."
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
