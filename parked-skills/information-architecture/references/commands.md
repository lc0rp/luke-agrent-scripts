# IA Commands Reference

- Link check: `pnpm run lint:links`
- Link auto-fix (wikilinks → markdown): `pnpm run lint:links:fix`
- IA shape check (folders + indexes + no wikilinks): `pnpm run lint:ia`

Scripts live in `scripts/link-check.mjs` and `scripts/ia-validate.mjs`.
